# ===========================================
# MultiZlogin Environment Configuration
# ===========================================

# Server Configuration
PORT=3000
NODE_ENV=production

# Session Secret (REQUIRED - Generate a random string)
SESSION_SECRET=your-super-secret-session-key-change-this

# Admin Default Password (Only used on first run)
# After first run, change password via UI
ADMIN_DEFAULT_PASSWORD=admin

# ===========================================
# Webhook Configuration
# ===========================================

# Webhook for incoming messages
MESSAGE_WEBHOOK_URL=

# Webhook for group events (join, leave, etc.)
GROUP_EVENT_WEBHOOK_URL=

# Webhook for message reactions
REACTION_WEBHOOK_URL=

# Webhook triggered when login is successful
WEBHOOK_LOGIN_SUCCESS=

# ===========================================
# Data Persistence (for Docker/Dokploy)
# ===========================================

# Path to store data (cookies, proxies, etc.)
# In Docker: /app/data
# In local: ./data
DATA_PATH=./data

# ===========================================
# Proxy Configuration
# ===========================================

# Maximum accounts per proxy
MAX_ACCOUNTS_PER_PROXY=3